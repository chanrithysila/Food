<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_navigation"
    app:startDestination="@id/listItemFragment">

    <fragment
        android:id="@+id/listItemFragment"
        android:name="com.example.calculatelistcard.FoodFragment"
        android:label="fragment_list_item"
        tools:layout="@layout/fragment_food" >
        <action
            android:id="@+id/action_listItemFragment_to_viewCardFragment"
            app:destination="@id/viewCardFragment" />
    </fragment>
    <fragment
        android:id="@+id/viewCardFragment"
        android:name="com.example.calculatelistcard.ViewCardFragment"
        android:label="fragment_view_card"
        tools:layout="@layout/fragment_view_card" />
</navigation>